---
import Layout from '../layouts/Layout.astro';
---

<Layout title="404 - Seite nicht gefunden">
    <!-- Navbar -->
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-logo">
                <span class="logo-icon">✨</span>
                <span>Nico</span>
            </a>
            <button class="nav-toggle" id="navToggle" aria-label="Toggle menu">
                <span class="hamburger"></span>
            </button>
            <ul class="nav-menu" id="navMenu">
                <li class="nav-item">
                    <a href="/" class="nav-link">
                        <span class="nav-icon">🏠</span>
                        Home
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/#experiences" class="nav-link">
                        <span class="nav-icon">💻</span>
                        Fähigkeiten
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/#projects" class="nav-link">
                        <span class="nav-icon">🚀</span>
                        Projekte
                    </a>
                </li>
                <li class="nav-item">
                    <a href="https://github.com/Nico4devv" target="_blank" class="nav-link">
                        <span class="nav-icon">⭐</span>
                        GitHub
                    </a>
                </li>
                <li class="nav-item">
                    <a href="/contact" class="nav-link nav-link-special">
                        <span class="nav-icon">📧</span>
                        Kontakt
                    </a>
                </li>
            </ul>
        </div>
    </nav>

    <main>
        <div class="error-container fade-in">
            <div class="error-content">
                <div class="error-icon">🔍</div>
                <div class="error-code">404</div>
                <h1>Seite <span class="text-gradient">nicht gefunden</span></h1>
                <p class="error-description">
                    Die gesuchte Seite existiert leider nicht oder wurde verschoben.<br />
                    Vielleicht findest du, was du suchst, auf einer dieser Seiten:
                </p>

                <div class="error-links">
                    <a href="/" class="error-link">
                        <span class="link-icon">🏠</span>
                        <div class="link-content">
                            <h3>Startseite</h3>
                            <p>Zurück zur Hauptseite</p>
                        </div>
                    </a>
                    <a href="/#projects" class="error-link">
                        <span class="link-icon">🚀</span>
                        <div class="link-content">
                            <h3>Projekte</h3>
                            <p>Meine Arbeiten ansehen</p>
                        </div>
                    </a>
                    <a href="/contact" class="error-link">
                        <span class="link-icon">📧</span>
                        <div class="link-content">
                            <h3>Kontakt</h3>
                            <p>Schreib mir eine Nachricht</p>
                        </div>
                    </a>
                </div>

                <a href="/" class="back-btn">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <line x1="19" x2="5" y1="12" y2="12"/>
                        <polyline points="12 19 5 12 12 5"/>
                    </svg>
                    <span>Zurück zur Startseite</span>
                </a>
            </div>
        </div>
    </main>
</Layout>

<style>
    /* Navbar - Same as contact.astro */
    .navbar {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        background: rgba(10, 10, 30, 0.85);
        backdrop-filter: blur(20px);
        z-index: 1000;
        padding: 1rem 0;
        border-bottom: 1px solid rgba(168, 85, 247, 0.2);
        box-shadow: 0 4px 30px rgba(168, 85, 247, 0.15);
    }

    .nav-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .nav-logo {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 1.6rem;
        font-weight: 800;
        color: white;
        text-decoration: none;
        transition: all 0.3s ease;
        z-index: 1001;
    }

    .logo-icon {
        font-size: 2rem;
        animation: float 3s ease-in-out infinite;
        filter: drop-shadow(0 0 10px rgba(168, 85, 247, 0.5));
    }

    @keyframes float {
        0%, 100% { transform: translateY(0px) rotate(0deg); }
        50% { transform: translateY(-8px) rotate(5deg); }
    }

    .nav-logo span:not(.logo-icon) {
        background: linear-gradient(135deg, #a855f7 0%, #22c55e 50%, #a855f7 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-size: 200% 200%;
        animation: gradientShift 4s ease infinite;
    }

    @keyframes gradientShift {
        0%, 100% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
    }

    .nav-logo:hover {
        transform: scale(1.08);
        filter: drop-shadow(0 0 20px rgba(168, 85, 247, 0.5));
    }

    .nav-toggle {
        display: none;
        flex-direction: column;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0.5rem;
        z-index: 1001;
    }

    .hamburger,
    .hamburger::before,
    .hamburger::after {
        width: 28px;
        height: 3px;
        background: linear-gradient(90deg, #a855f7, #22c55e);
        border-radius: 3px;
        transition: all 0.3s ease;
    }

    .hamburger {
        position: relative;
    }

    .hamburger::before,
    .hamburger::after {
        content: '';
        position: absolute;
        left: 0;
    }

    .hamburger::before { top: -9px; }
    .hamburger::after { top: 9px; }

    .nav-toggle.active .hamburger { background: transparent; }
    .nav-toggle.active .hamburger::before { transform: rotate(45deg); top: 0; }
    .nav-toggle.active .hamburger::after { transform: rotate(-45deg); top: 0; }

    .nav-menu {
        display: flex;
        gap: 0.75rem;
        list-style: none;
        margin: 0;
        padding: 0;
        align-items: center;
    }

    .nav-item {
        position: relative;
    }

    .nav-link {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        color: rgba(255, 255, 255, 0.9);
        text-decoration: none;
        font-weight: 600;
        font-size: 1rem;
        padding: 0.85rem 1.5rem;
        border-radius: 14px;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(255, 255, 255, 0.08);
    }

    .nav-link::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(168, 85, 247, 0.15) 0%, rgba(34, 197, 94, 0.15) 100%);
        opacity: 0;
        transition: opacity 0.3s ease;
        border-radius: 14px;
    }

    .nav-link:hover::before { opacity: 1; }

    .nav-link:hover {
        color: white;
        transform: translateY(-3px);
        border-color: rgba(168, 85, 247, 0.3);
        box-shadow: 0 8px 25px rgba(34, 197, 94, 0.3);
    }

    .nav-icon {
        font-size: 1.3rem;
        transition: transform 0.3s ease;
    }

    .nav-link:hover .nav-icon {
        transform: scale(1.25) rotate(8deg);
    }

    .nav-link-special {
        background: linear-gradient(135deg, #a855f7 0%, #22c55e 100%);
        color: white;
        border: none;
        box-shadow: 0 4px 20px rgba(168, 85, 247, 0.4);
        font-weight: 700;
    }

    .nav-link-special:hover {
        box-shadow: 0 8px 30px rgba(34, 197, 94, 0.6);
        transform: translateY(-3px) scale(1.05);
    }

    /* Main Content */
    main {
        margin: auto;
        padding: 8rem 2rem 3rem;
        max-width: 900px;
        width: 100%;
        min-height: 100vh;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .error-container {
        text-align: center;
        width: 100%;
    }

    .error-content {
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid rgba(168, 85, 247, 0.2);
        border-radius: 24px;
        padding: 4rem 3rem;
        backdrop-filter: blur(10px);
    }

    .error-icon {
        font-size: 5rem;
        margin-bottom: 1rem;
        animation: bounce 2s ease-in-out infinite;
        display: inline-block;
        filter: drop-shadow(0 0 20px rgba(168, 85, 247, 0.6));
    }

    @keyframes bounce {
        0%, 100% {
            transform: translateY(0) scale(1);
        }
        50% {
            transform: translateY(-20px) scale(1.1);
        }
    }

    .error-code {
        font-size: clamp(6rem, 15vw, 10rem);
        font-weight: 900;
        background: linear-gradient(135deg, #a855f7 0%, #22c55e 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        line-height: 1;
        margin-bottom: 1rem;
        animation: floatCode 3s ease-in-out infinite;
    }

    @keyframes floatCode {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-15px); }
    }

    h1 {
        font-size: clamp(2rem, 5vw, 3rem);
        font-weight: 900;
        line-height: 1.2;
        margin-bottom: 1.5rem;
        letter-spacing: -0.02em;
    }

    .text-gradient {
        background-image: linear-gradient(135deg, #a855f7 0%, #22c55e 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .error-description {
        font-size: clamp(1rem, 2.5vw, 1.1rem);
        color: rgba(255, 255, 255, 0.7);
        line-height: 1.7;
        margin-bottom: 3rem;
    }

    .error-links {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
        margin-bottom: 3rem;
    }

    .error-link {
        display: flex;
        align-items: center;
        gap: 1rem;
        padding: 1.5rem;
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(255, 255, 255, 0.08);
        border-radius: 16px;
        text-decoration: none;
        transition: all 0.3s ease;
        text-align: left;
    }

    .error-link:hover {
        background: rgba(168, 85, 247, 0.08);
        border-color: rgba(168, 85, 247, 0.3);
        transform: translateY(-5px);
        box-shadow: 0 8px 25px rgba(168, 85, 247, 0.3);
    }

    .link-icon {
        font-size: 2rem;
        flex-shrink: 0;
    }

    .link-content h3 {
        font-size: 1.1rem;
        font-weight: 700;
        color: white;
        margin-bottom: 0.3rem;
    }

    .link-content p {
        font-size: 0.9rem;
        color: rgba(255, 255, 255, 0.6);
        margin: 0;
    }

    .back-btn {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1.2rem 2.5rem;
        background: linear-gradient(135deg, #a855f7 0%, #22c55e 100%);
        color: white;
        text-decoration: none;
        border-radius: 14px;
        font-weight: 700;
        font-size: 1.05rem;
        transition: all 0.3s ease;
        box-shadow: 0 4px 20px rgba(168, 85, 247, 0.4);
    }

    .back-btn:hover {
        transform: translateY(-3px);
        box-shadow: 0 8px 30px rgba(34, 197, 94, 0.6);
    }

    /* Animations */
    .fade-in {
        opacity: 0;
        animation: fadeInUp 1s ease forwards;
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(40px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* Responsive */
    @media (max-width: 768px) {
        .nav-toggle {
            display: flex;
        }

        .nav-menu {
            position: fixed;
            top: 0;
            right: -100%;
            width: 300px;
            height: 100vh;
            background: linear-gradient(180deg, rgba(15, 10, 30, 0.98) 0%, rgba(10, 10, 25, 0.98) 100%);
            backdrop-filter: blur(25px);
            flex-direction: column;
            padding: 6rem 2rem 2rem;
            gap: 0.75rem;
            transition: right 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            box-shadow: -10px 0 50px rgba(0, 0, 0, 0.7);
            border-left: 2px solid rgba(168, 85, 247, 0.3);
        }

        .nav-menu.active {
            right: 0;
        }

        .nav-link {
            width: 100%;
            justify-content: flex-start;
            padding: 1.2rem 1.8rem;
            font-size: 1.15rem;
        }

        .nav-icon {
            font-size: 1.5rem;
        }

        main {
            padding: 6rem 1rem 2rem;
        }

        .error-content {
            padding: 2.5rem 1.5rem;
        }

        .error-links {
            grid-template-columns: 1fr;
        }

        .error-description br {
            display: none;
        }
    }
</style>

<script>
    const navToggle = document.getElementById('navToggle');
    const navMenu = document.getElementById('navMenu');

    navToggle?.addEventListener('click', () => {
        navMenu?.classList.toggle('active');
        navToggle?.classList.toggle('active');
    });

    document.addEventListener('click', (e) => {
        const target = e.target as HTMLElement;
        if (!target.closest('.nav-container')) {
            navMenu?.classList.remove('active');
            navToggle?.classList.remove('active');
        }
    });
</script>